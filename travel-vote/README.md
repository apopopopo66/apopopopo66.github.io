# Travel Vote

LINEで共有して、5人程度のグループで宿を5段階評価するモバイル向けミニWebアプリです。既存サイトとは独立して `/travel-vote/` 配下だけで動作します。

## 構成

- `index.html` — 画面
- `styles.css` — iPhone/LINE内ブラウザを優先したスタイル
- `app.js` — 評価、保存、集計、共有
- `data.js` — 初期候補データ
- `config.js` — 公開用Supabase設定（秘密情報は置かない）
- `config.example.js` — Supabase設定例
- `supabase.sql` — テーブル、RLS、初期データ

## リリースルール（必須）

変更内容を確認しただけでリリース完了とはしない。**mainへ反映する前、またはmainを直接更新した場合は公開完了と案内する前に、最低限以下の単体テスト相当・スモークテストを実施する。** テスト未実施または失敗中の状態を「反映済み」「修正済み」とユーザーへ案内しない。

### レビュー観点

1. **要件反映** — ユーザーが依頼した文言、候補、番号、価格、1人目安、画像、リンクが実データと画面表示ロジックの両方に反映されているか。
2. **データ整合性** — `data.js` の候補IDが重複していないか。削除済み候補が残っていないか。料金の割り算・人数・日付が一致しているか。
3. **表示整合性** — 宿名の番号が宿カード、集計結果、メンバー別表で同一番号になっているか。候補順変更時も番号が自動追従するか。
4. **画像** — `imageUrl` が実際に画像を返すか。別施設の写真ではないか。表示不能URLや古いURLを設定していないか。
5. **保存/読込** — 新規評価、評価更新、評価解除、再読込後の復元が動くか。削除済み候補の古い投票データが保存処理を壊さないか。
6. **集計** — 平均点、人数、並び順、トップ表示、メンバー別表が投票データと一致するか。
7. **キャッシュ** — `app.js` / `data.js` / `styles.css` / `config.js` を変更した場合、`index.html` のキャッシュバスターも更新されているか。公開ページが最新ファイルを読み込んでいるか。
8. **公開確認** — GitHub上の最新コードだけでなく、GitHub Pagesの公開URLから最新表示を確認する。公開反映前は完了扱いにしない。
9. **モバイル** — 320px幅、iPhone Safari/LINE内ブラウザ相当で主要操作が可能で、横崩れしないか。
10. **回帰** — 今回変更していない投票保存、集計、共有、既存候補の表示が壊れていないか。

### リリース前テスト（最低ライン）

- [ ] `data.js` の候補件数・順番・ID・名称を確認
- [ ] 各候補に `候補1` から連番が表示される
- [ ] 宿カードと集計結果とメンバー別表の番号が一致する
- [ ] 5人料金と1人目安の計算が一致する
- [ ] 全画像URLが取得可能で対象施設の画像である
- [ ] 全詳細リンクが対象施設へ遷移する
- [ ] 星1〜5の選択と評価解除ができる
- [ ] 名前未入力時に保存を拒否する
- [ ] 新規評価を保存できる
- [ ] 同じ名前の評価を更新できる
- [ ] 評価解除を保存できる
- [ ] 再読込後に名前と評価が復元される
- [ ] 平均点・評価人数・トップ表示が正しい
- [ ] 削除済み候補の古いデータがあっても保存・集計できる
- [ ] ConsoleにJavaScriptエラーがない
- [ ] JS/CSS/data/config変更時にキャッシュバスターを更新済み
- [ ] GitHub Pages公開後、公開URLで最新内容を確認済み

**上記の必須項目が確認できない変更はリリースしない。**

## Supabase 初期設定

1. SupabaseでProjectを作成する。
2. SQL Editorで `supabase.sql` を実行する。
3. Project Settings / API から Project URL と anon/publishable key を確認する。
4. `config.js` にProject URLとanon/publishable keyを設定する。

anon/publishable key はブラウザ公開を前提としたキーであり、service-role key のような秘密鍵ではありません。このアプリの認可はRLSに依存します。**service-role key は絶対にコミットしないでください。**

### RLSのトレードオフ

この用途は友人5人へのURL共有を想定し、ログインなしで `votes` の匿名SELECT/INSERT/UPDATE/DELETEを許可しています。そのため、URLを知っている第三者は名前を偽って投票を書き換えることができます。厳密な本人性が必要になった場合はSupabase Authを導入し、`auth.uid()` ベースのRLSへ変更してください。候補宿の更新は匿名ユーザーには許可していません。

## GitHub Pages

公開URLは `/travel-vote/`。変更後はGitHub上のファイル更新だけで完了とせず、Pagesへのデプロイ完了と公開ページの表示確認までをリリース作業に含めます。

## デモ状態

`config.js` が空のままでも画面はクラッシュせず、候補の閲覧とローカルでの星選択ができます。共同保存・集計は無効で、画面に設定が必要であることを表示します。

## 手動スモークテスト

上記「リリース前テスト（最低ライン）」を実施します。特に変更箇所だけでなく、保存→再読込→集計までの主要導線を毎回確認します。

## 現在の候補

BROS RESORT KYONAN / BASK HOTA / D6 Makiwarina 薪割りーな / Ocean Sauna Villa 富津竹岡 by GIFTHOUSE。

価格・設備情報は比較用の目安です。予約前に各施設の最新情報を確認してください。
